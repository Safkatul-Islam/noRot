import React from 'react';
import ReactDOM from 'react-dom/client';
import { VoiceOrbOverlayPage } from './pages/VoiceOrbOverlayPage';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VoiceOrbOverlayPage />
  </React.StrictMode>
);
