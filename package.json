{
  "name": "norot",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:desktop": "npm -w @norot/shared run build && npm -w @norot/desktop run dev",
    "dev:api": "cd apps/api && uvicorn app.main:app --reload --port 8000",
    "build": "npm -w @norot/shared run build && npm -w @norot/desktop run build",
    "dist:desktop": "npm -w @norot/shared run build && npm -w @norot/desktop run dist"
  },
  "overrides": {
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3"
  }
}
